// import { Question } from "../../models/questions.js";

// export async function getCodingQuestions({ recruiterId } = {}) {
//   const query = { testType: "coding" };
//   if (recruiterId) {
//     query.createdBy = recruiterId;
//   }
//   return Question.find(query).sort({ createdAt: -1 });
// }
// In progress
// export async function createCodingQuestion(req, res)
       