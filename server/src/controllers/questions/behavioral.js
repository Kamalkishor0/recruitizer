// import { Question } from "../../models/questions.js";

// export async function getBehavioralQuestions({ recruiterId } = {}) {
//   const query = { testType: "behavioral" };
//   if (recruiterId) {
//     query.createdBy = recruiterId;
//   }
//   return Question.find(query).sort({ createdAt: -1 });
// }

// In progress
// export async function createBehavioralQuestion(req, res)